<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Feedback | Tech People</title>
		<link rel="icon" href="/favicon.svg" />
		<link
			href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap"
			rel="stylesheet"
		/>
		<style>
			:root {
				--primary: #6366f1;
				--background: #f9fafb;
				--foreground: #ffffff;
				--text-color: #1f2937;
				--border: #e5e7eb;
				--danger: #ef4444;
				--gray: #6b7280;
			}

			.toast {
				position: fixed;
				bottom: 20px;
				left: 50%;
				transform: translateX(-50%);
				background-color: #28a745;
				color: white;
				padding: 16px;
				border-radius: 8px;
				opacity: 0;
				transition: opacity 0.5s ease-in-out;
				z-index: 1000;
				font-family: sans-serif;
			}

			.toast-error {
				background-color: #dc3545;
			}

			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			body {
				font-family: "Inter", sans-serif;
				background-color: var(--background);
				color: var(--text-color);
				padding: 2rem;
			}

			header {
				background-color: var(--primary);
				padding: 2rem;
				text-align: center;
				color: white;
				font-size: 1.5rem;
				font-weight: 600;
				border-radius: 8px;
				margin-bottom: 2rem;
			}

			.container-alert {
				background-color: var(--danger);
				color: white;
				padding: 1rem;
				text-align: center;
				border-radius: 6px;
				margin-bottom: 2rem;
			}

			.feedback-container {
				background-color: var(--foreground);
				max-width: 640px;
				margin: 0 auto;
				padding: 2rem;
				border-radius: 12px;
				box-shadow: 0 8px 20px rgba(0, 0, 0, 0.05);
			}

			.feedback-container h1 {
				text-align: center;
				font-size: 1.75rem;
				margin-bottom: 1.5rem;
			}

			form {
				display: flex;
				flex-direction: column;
				gap: 1.5rem;
			}

			fieldset {
				border: none;
			}

			legend {
				font-size: 1.1rem;
				font-weight: 600;
				margin-bottom: 0.5rem;
				color: var(--gray);
			}

			label {
				display: block;
				margin-bottom: 0.5rem;
				font-weight: 500;
			}

			input,
			textarea {
				width: 100%;
				padding: 0.75rem;
				border: 1px solid var(--border);
				border-radius: 6px;
				font-size: 1rem;
				background-color: #f3f4f6;
			}

			textarea {
				resize: vertical;
			}

			input:focus,
			textarea:focus {
				outline: none;
				border-color: var(--primary);
				background-color: white;
			}

			.button-group {
				display: flex;
				justify-content: flex-start;
				gap: 1rem;
				margin-top: 1.5rem; /* Moved up from the bottom */
			}

			input[type="submit"],
			input[type="reset"] {
				padding: 0.75rem 1.5rem;
				font-weight: 600;
				border: none;
				border-radius: 6px;
				cursor: pointer;
				transition: background 0.3s;
			}

			input[type="submit"] {
				background-color: var(--primary);
				color: white;
			}

			input[type="submit"]:hover {
				background-color: #4f46e5;
			}

			input[type="reset"] {
				background-color: var(--gray);
				color: white;
			}

			input[type="reset"]:hover {
				background-color: #4b5563;
			}

			@media (max-width: 480px) {
				.button-group {
					flex-direction: column;
					width: 100%;
				}
				input[type="submit"],
				input[type="reset"] {
					width: 100%;
				}
			}
		</style>
	</head>
	<body>
		<header>Feedback for Techtack-Technologies or Team-Litho</header>

		<div class="container-alert">
			Please fill out the form below to provide your feedback. Your input
			helps us improve.
		</div>

		<div class="feedback-container">
			<h1>Feedback Form</h1>
			<form id="QZD2eWWqxtOhJPC9Qe7K" method="POST">
				<fieldset>
					<legend>Feedback (required)</legend>
					<label for="feedback">Your feedback:</label>
					<textarea
						name="feedback"
						id="feedback"
						rows="5"
						placeholder="Write your feedback here..."
						required></textarea>
				</fieldset>

				<fieldset>
					<legend>Contact Information</legend>
					<label for="email">Email (required):</label>
					<input
						type="email"
						id="email"
						name="email"
						placeholder="you@example.com"
						required
					/>

					<label for="telephone">Telephone (required):</label>
					<input
						type="tel"
						id="telephone"
						name="telephone"
						placeholder="123-456-7890"
						required
					/>
				</fieldset>

				<fieldset>
					<legend>Personal Information</legend>
					<label for="first_name">First Name (required):</label>
					<input
						type="text"
						id="first_name"
						name="first_name"
						placeholder="John"
						required
					/>

					<label for="last_name">Last Name (required):</label>
					<input
						type="text"
						id="last_name"
						name="last_name"
						placeholder="Doe"
						required
					/>
				</fieldset>

				<div class="button-group">
					<input type="submit" value="Send" />
					<input type="reset" value="Reset" />
				</div>
			</form>
		</div>

		<script type="module" src="../../public/script/feedback.ts"></script>
		<script type="module" src="../../public/script/firebase.ts"></script>
	</body>
</html>
